<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="airbnb.spring.mapper.LoginMapper">
	
	<select id="login" resultType="airbnb.spring.dto.User">
		select * from ousers where id=#{id}
	</select>

	<select id="userRole" resultType="String">
		select role_id from ousers_role where id=#{id}
	</select>
	
	<!--자동로그인시 세션키값을 유저 테이블에 저장 합니다 -->
	<update id="updateSessionKey">
		update ousers set sessionkey= #{sessionkey}
                ,sessionlimit = sysdate + 7
		where id = #{id}
	</update>
	
	<!-- sessionkey 가져와서 로그인 -->
	<select id="loginSessionKey" resultType="airbnb.spring.dto.User">
		select * from ousers 
				where sessionkey= #{sessionkey} 
                and sessionlimit > sysdate
	</select>
	
	<select id="checkId" resultType="airbnb.spring.dto.User">
		select * from ousers where id = #{id}
	</select>
	
	<insert id="insertUser">
		insert into ousers (id, pwd, enabled, name, email, tel)
			values (#{id}, #{pwd}, 1, #{name}, #{email}, #{tel})
	</insert>
	
	<insert id="insertUserRole">
		insert into ousers_role values (#{id}, #{role})
	</insert>
	
	<select id="searchId" resultType="airbnb.spring.dto.User">
		select id from ousers where name = #{name} or email = #{email}
	</select>
	
	<select id="searchPwd" resultType="airbnb.spring.dto.User">
	select * from ousers where id = #{id} or name = #{name}
    or email = #{email}	
    </select>
	
	<update id="updatePwd">
	update ousers set pwd=#{pwd} where id=#{id} or name=#{name} 
	or email=#{email} 
	</update>
	
	<select id="get" resultType="airbnb.spring.dto.User">
		select * from ousers where id=#{id}
	</select>
	
</mapper>